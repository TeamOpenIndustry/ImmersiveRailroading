import:
  source = immersiverailroading:gui/default/steam/gauge_common_face.caml
  replace =
    TITLE = TEMPERATURE
    MIN = 0
    CURRENT = stat.temperature stat.units_temperature
    MAX = stat.max_temperature

import:
  source = immersiverailroading:gui/default/steam/gauge_dial.caml
  replace =
    READOUT = TEMPERATURE